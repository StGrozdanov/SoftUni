function findLargestOfThreeNums(number1, number2, number3){
    let largestNumber = Math.max(number1, number2, number3);

    console.log(`The largest number is ${largestNumber}.`)
}

findLargestOfThreeNums(5, -3, 16);